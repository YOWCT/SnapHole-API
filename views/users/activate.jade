extends ../partials/layout.jade
block content
    h1= title
    if status == "Valid"
        p Hi #{user.first_name} #{user.last_name},
        p Thank you for registering to #{APP_NAME}, please set a password for your account. 
        .col-md-4
            form(action="#{user.token}" method="post")
                .form-group
                    label(for="pass1") Password
                    input#pass1.form-control(type="password" placeholder='Password' name="password")
                .form-group
                    label(for="pass1") Confirm password
                    input#pass1.form-control(type="password" placeholder='Confirm password' name="pass2")
                input(type="submit")
    else
        p 😡 😡 😡 😡 😡
    script.
        window.location = 'snapHole://'; // will result in error message if app not installed
        setTimeout(function() {
        // Link to the App Store should go here -- only fires if deep link fails                
        window.location = "https://itunes.apple.com/us/app/myapp/id123456789?ls=1&mt=8";
        }, 500);